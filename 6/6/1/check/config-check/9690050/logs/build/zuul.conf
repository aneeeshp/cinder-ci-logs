[scheduler]
tenant_config=main.yaml

[zookeeper]
hosts=localhost:2281
tls_cert=/etc/ssl/zookeeper.crt
tls_key=/etc/ssl/zookeeper.key
tls_ca=/etc/ssl/zk-ca.pem

[keystore]
password=secret

[database]
dburi=sqlite://

[connection gerrit]
driver=gerrit
server=localhost
user=zuul

[connection review.opendev.org]
driver=gerrit
server=localhost
user=zuul






[connection smtp]
driver=smtp
server=localhost

